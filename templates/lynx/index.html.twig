{% extends 'lynx/base.html.twig' %}

{% block title %}Hello peeps!{% endblock %}

{% block head %}
  <div class="jumbotron jumbotron-fluid">
    <div class="container">
      <h1>LyNX</h1>
      <sub>Keep track of all kinds of links</sub>
    </div>
  </div>
{% endblock head %}

{#
{% block body %}
  <form action="{{ path("lynx_post_link") }}" method="POST">
    <div class="form-group">
      <label for="new-link">Add link</label>
      <input type="text" class="form-control" id="newlink">
    </div>
    <button type="submit" class="btn btn-primary">Add link</button>
  </form>
{% endblock %} 
#}

{% block body %}
  {{ form_start(form, {'method': 'POST'} ) }}

  <div class="form-group">
    {{ form_label(form.url) }}
    {{ form_errors(form.url) }}
    {{ form_widget(form.url, {'attr': {'class': 'form-control'} }) }}
  </div>

  <div class="form-group">
    <input type="submit" value="Bewaar" class="form-control">
  </div>

  {{ form_end(form) }}
{% endblock body %}

