{# embeddable template: all links #}

{% if links|length > 0 %}
  <ul>
    {% for link in links %}
      <li class="row" data-link-id="{{ link.id }}">
        <span class="col-lg-11">{{ link.url }} </span>
        <span class="edit-link col-lg-1">Wijzig</span>
      </li>
    {% endfor %}
  </ul>
{% endif %}
